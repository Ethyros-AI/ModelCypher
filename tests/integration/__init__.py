"""Integration tests for ModelCypher workflows.

Tests end-to-end workflows that span multiple domain modules:
- Merge pipeline: profile → guide → merge → validate
- Entropy workflow: probe → measure → classify → detect
"""
